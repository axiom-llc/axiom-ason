[project]
name = "axiom-ason"
version = "0.2.0"
description = "ASON — Autonomous Security Optimization Network"
authors = [{name = "AXIOM LLC"}]
requires-python = ">=3.11"
dependencies = ["axiom-apex>=3.1.0", "pydantic>=2"]

[project.scripts]
ason = "ason.__main__:main"
