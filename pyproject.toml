[project]
name = "axiom-aoson"
version = "0.1.0"
description = "AOSON — Autonomous Security Optimization Network"
authors = [{name = "AXIOM LLC"}]
requires-python = ">=3.11"
dependencies = ["axiom-apex"]

[project.scripts]
aoson = "aoson.__main__:main"
